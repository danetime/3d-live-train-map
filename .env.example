# Realtime Trains (RTT) API credentials — Phase 2.
# Register at https://api-portal.rtt.io/ (free, non-commercial), then copy this
# file to ".env" and fill these in. ".env" is gitignored; these stay server-side
# (injected by the Vite dev proxy / your serverless function), never the browser.
RTT_USERNAME=
RTT_PASSWORD=
